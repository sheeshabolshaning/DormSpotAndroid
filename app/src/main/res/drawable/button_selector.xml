<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state -->
    <item android:state_selected="true" android:drawable="@drawable/rounded_selected" />
    <!-- Pressed state -->
    <item android:state_pressed="true" android:drawable="@drawable/rounded_selected" />
    <!-- Default state -->
    <item android:drawable="@drawable/rounded_selector" />
</selector>
